# algorithm

# Generate FIRST set

# check the first symbol
# if it is a temrinal
  # add it to the set
# check if we have a '|' sign 
  # if so, repeat the process

# if it is a non_terminal
  # check that non_terminal to see if they have a terminal


# what do i need?
  # as a key i would have the non_terminal
  # as a value an array with the ORs
  # inside each array element, the sybols